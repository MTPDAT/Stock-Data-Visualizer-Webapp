services:
  stock-visualizer:
    build: .
    ports:
      - "5005:5005"
    environment:
      - MPLBACKEND=Agg
      - SECRET_KEY=your-secret-key-here
    volumes:
      - ./stocks.csv:/app/stocks.csv
    restart: unless-stopped
